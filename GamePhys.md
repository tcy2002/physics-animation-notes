# 游戏物理仿真

## 沙地/雪地

业内最常用的非物理仿真的沙地/雪地足迹方案：
通过“顶部相机 + RenderTexture”记录足部轨迹，再把轨迹写入运行时高度图，对沙地网格做高度偏移，最后按时间自动恢复。过程如下：

- 获取脚印数据：
  - 方法1：正交相机深度捕捉：ortho模式，只渲染角色的脚，或专门绑定的碰撞体，绘制到一张RT上
  - 方法2：笔刷绘制，当角色脚或碰撞体接触地面时，在接触点生成一个脚印笔刷，通过脚本绘制到RT上
- 写入渲染目标：
  - 新的脚印深度混合到RT中，可根据当前深度计算新的深度变化，呈现逐步“压实”的效果
  - 随着时间推移，根据恢复参数，或天气参数，RT中的塌陷区域会逐渐升高，从而可模拟雪重新覆盖的效果

通常地面网格的顶点量很少，不足以表现细腻边缘，需要使用动态细分：把接触到的表面位置绘制到一个深度图/RT里，在Tessellation Shader根据深度信息RT进行细分

## 布料

目前最常用的是骨骼驱动的布料仿真，角色衣物并非完全由物理引擎接管，首先会根据角色的骨骼运动状态，用Skinning得到跟随角色变化的衣物顶点位置，物理引擎计算相对于这个基础位置的偏移量。优点：保证角色在做剧烈运动时，衣服不会严重走形或穿模

也可将物理驱动和骨骼驱动分开（设计一个混合权重，0为Skinning，1为完全物理模拟）：贴身衣物为Skinning骨骼驱动，细节飘带为PBD仿真，加上一些外围碰撞体以防止与自身穿模

## 开放水面

[fft海面模拟(一)](https://zhuanlan.zhihu.com/p/64414956)
[fft海面模拟(二)](https://zhuanlan.zhihu.com/p/64726720)
[fft海面模拟(三)](https://zhuanlan.zhihu.com/p/65156063)

游戏很少用纯物理仿真的水体模拟，开放水面一般用FFT模拟来做，核心是利用海洋统计频谱生成频域波浪数据，通过IFFT（快速傅里叶逆变换）或IDFT（离散傅里叶逆变换）快速转换为海面高度场

相比于传统Gerstner波，FFT海面可轻松叠加非常多组波浪，细节答复提升

本质：海面高度场 $h(x,z,t)$ 可表示为无数不同频率、振幅、相位的复指数正弦信号线形叠加：

$$h(x,z,t)=\sum_m \sum_h \hat h(k,t) \cdot e^{ik \cdot (x,z)}$$

真实感的来源是频谱，海洋统计学通用的频谱是菲利普频谱：

### 菲利普频谱

- 初始频谱：

$$\tilde{h}_0(\boldsymbol{k}) = \frac{\xi_r + i\cdot\xi_i}{\sqrt{2}} \cdot \sqrt{P_h(\boldsymbol{k})}$$

  `ξr、ξi`：相互独立的标准正态分布随机数（均值0，标准差1），共轭约束：`tilde{h}_0(-k) = tilde{h}_0*(k)`，保证生成的高度值是纯实数

- 能量核心：

$$P_h(\boldsymbol{k}) = A \cdot \frac{e^{-1/(kL)^2}}{k^4} \cdot (\boldsymbol{k}\cdot\boldsymbol{w})^2$$

  `A`：波浪振幅常量，控制整体浪高；`g`为重力加速度；`V`为风速；`L=V²/g`，为风速对应的最大波浪波长；`w`：风向单位向量，`(k·w)²`保证波浪能量主要集中在风向方向，完美模拟风驱海面的效果；`1/k⁴`项：实现长波能量高、短波能量快速衰减的特性，符合真实海洋的波浪分布

- 时间演化：

$$\tilde{h}(\boldsymbol{k},t) = \tilde{h}_0(\boldsymbol{k})e^{i\omega(\boldsymbol{k})t} + \tilde{h}_0^*(-\boldsymbol{k})e^{-i\omega(\boldsymbol{k})t}$$

  `ω(k)=√(gk)`为深水波的色散关系，决定不同波长波浪的传播速度：长波传播更快，短波更慢

# 性能优化和工程细节

暴力计算IDFT的时间复杂度为 $O(N^2)$ ，但基于分治思想可将复杂度优化到 $O(N log N)$，思路：将N点DFT拆分为2个N/2点的DFT（偶数序号输入、奇数序号输入），再通过旋转因子合并结果，递归拆分直到最小的2点DFT

递归形式的FFT不适合GPU实现，因此需要展开为迭代形式的蝶形网络。将递归FFT完全展开为固定的迭代计算网络，N点FFT有`logN`个计算阶段，每个阶段可并行处理N个数据

二维IFFT的行与行之间、列与列之间完全独立，N×N的网格中，行IFFT的N个行可完全并行，列IFFT的N个列可完全并行；GPU实现下，整体计算量仅相当于2个N点一维IFFT

详细步骤：
详细步骤定义：
1.  预计算：根据菲利普频谱公式，生成所有频域点的初始频谱`tilde{h}_0(k)`，并预计算色散关系`ω(k)`，可一次性完成，无需每帧重复计算。
2.  时间更新：根据当前时间t，计算随时间演化的频域频谱`tilde{h}(k,t)`，每帧仅需逐元素运算，开销极低。
3.  符号校正1：对`tilde{h}(k,t)`乘以`(-1)^(m+n)`，完成第一次符号校正，得到适配IFFT的输入数据。
4.  行IFFT：对校正后的频域数据，按行执行一维IFFT，得到中间结果。
5.  符号校正2：对行IFFT的结果乘以`(-1)^(m+v)`，完成第二次符号校正。
6.  列IFFT：对校正后的数据，按列执行一维IFFT，得到第二次中间结果。
7.  符号校正3：对列IFFT的结果乘以`(-1)^(u+v)`，完成第三次符号校正，最终得到空域的海面高度场`h(x,z,t)`。
8.  渲染数据计算：基于高度场，同步计算Choppy尖浪的坐标偏移量、法线、雅可比行列式（泡沫判断）等渲染所需数据。
